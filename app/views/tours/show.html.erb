<h1> <%= link_to @tour.band.first.name, band_url(@tour.band) %> - <%= @tour.name %> </h1>
<%= link_to "Edit", edit_band_tour_url(@tour.band, @tour) %> |
<%= link_to "Delete", tour_url(@tour), method: :delete %>

<%= render 'events/list', events: @tour.events, hide_band_name: true %>